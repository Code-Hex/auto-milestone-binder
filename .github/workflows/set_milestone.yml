
on:
  pull_request:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Set closest minestone if not set
    steps:
    - uses: ku/auto-milestone-binder@v1.1.9
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        loose: '1'
