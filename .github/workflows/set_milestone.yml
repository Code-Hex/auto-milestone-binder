
on:
  pull_request:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: ku/auto-milestone-binder@v1.1.6
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        loose: '1'
